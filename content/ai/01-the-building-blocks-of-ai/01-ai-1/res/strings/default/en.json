{
  "concept.ai-llm.term": "Large language model",
  "concept.ai-llm.def": "A model that reads the text so far and predicts the next token, over and over.",
  "concept.ai-next-token-prediction.term": "Next-token prediction",
  "concept.ai-next-token-prediction.def": "For each next slot the model scores every possible token and usually picks the most likely one, then appends it and reads the whole text again."
}
