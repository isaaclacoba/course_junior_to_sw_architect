{
  "concept.ai-chain-of-thought.term": "Chain of thought",
  "concept.ai-chain-of-thought.def": "Asking the model to write its reasoning out step by step before answering - the working is the computation, not decoration, because each token it writes lands back in the context it reads next."
}
