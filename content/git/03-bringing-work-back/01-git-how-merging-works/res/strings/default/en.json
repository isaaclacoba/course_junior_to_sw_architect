{
  "card.title": "How merging works",
  "card.blurb": "Merging is not always the same move. Watch git slide a name forward without saving anything new, then watch it make the one commit that points back at two parents.",
  "hero.eyebrow": "Git · Part three · Bringing work back",
  "hero.title": "How merging works",
  "intro.0": "Bringing a branch back sounds like the big step - the one where git stitches two histories together and something gets broken. Often it does almost nothing: it slides a name forward and stops. Other times it saves one new snapshot that points back at both lines. Which one you get is not a setting you chose; it depends only on where the two names are sitting. Here you watch both happen, so you can tell them apart on sight.",
  "concept.gt-merge.term": "Merge",
  "concept.gt-merge.def": "Bringing another branch's commits into the one you are on, so two lines become one.",
  "concept.gt-fast-forward.term": "Fast-forward",
  "concept.gt-fast-forward.def": "When your branch has added nothing since the split, git just slides its name forward and makes no new commit.",
  "concept.gt-merge-commit.term": "Merge commit",
  "concept.gt-merge-commit.def": "The commit a merge makes when both sides moved on - the one commit with two parents.",
  "step.0.narr": "`main` has one commit, `add cat`. `fix` split off from it and saved `add dog` of its own, so `fix` is one commit ahead.\n\nYou are standing on `main` - that is what `HEAD` says. To merge, you name the branch you want to bring IN: `git merge fix`. The branch you are standing on is the one that moves.",
  "step.0.note": "`fix` is one commit ahead. `main` has not moved.",
  "step.1.narr": "Before running anything, look at where `main` sits: still on `add cat`, the exact commit `fix` grew out of. `main` has added nothing since the split. There are not two lines here at all - there is one line, and `main` is standing behind on it.",
  "step.1.note": "Nothing to combine - `main` is simply behind.",
  "step.2.narr": "`git merge fix`, and the name slides forward onto `fix`'s commit. **Nothing new was saved.** There was nothing to combine, so git took the shortcut - that shortcut is called a fast-forward.",
  "step.2.note": "The name moved. No new commit.",
  "step.3.narr": "Same story, one change: while `fix` was working, `main` saved `feed the cat` of its own. Now neither name is behind the other - each has a commit the other has never seen. Sliding `main` forward would leave its own commit off the line.",
  "step.3.note": "Both sides moved. Neither is behind.",
  "step.4.narr": "`git merge fix` again, and this time git saves a commit. Look at what it points back at: **two parents**, one on each side. That single commit is what joins the two lines, and it is the only kind with more than one parent.",
  "step.4.note": "One new commit, pointing back at both tips.",
  "step.5.narr": "You can read which one you got straight off the graph, without being told. A name that just moved forward, with no new dot, was a fast-forward. A dot with two lines running back out of it is a merge commit, and it is there because both sides had moved.",
  "step.5.note": "The shape tells you which merge happened.",
  "legend.0": "a commit - one saved snapshot",
  "legend.1": "a branch name",
  "legend.2": "HEAD - where you are"
}
