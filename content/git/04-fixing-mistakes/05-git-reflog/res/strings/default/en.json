{
  "card.title": "Find a lost commit",
  "card.blurb": "`git log` shows what a branch can still reach. `git reflog` shows every place `HEAD` has been, so a commit you thought you had thrown away is still findable by its hash.",
  "hero.eyebrow": "Git · Part four · Fixing mistakes",
  "hero.title": "Find a lost commit",
  "intro.0": "You amend a commit and the one you had a second ago is not in `git log` any more. It looks gone. Usually it is not: git keeps a second list of every place you have stood, and the old commit is still in there with its hash. Here you lose a commit on purpose and then get it back.",
  "task.1.title": "The commit git log forgot",
  "task.1.concept": "git reflog",
  "task.1.context": "Three commits, and the message on the last one says nothing useful. `git commit --amend` is the fix you already met.\n\nIt puts a new commit in place of the top one. The old commit stays in the repository - it only loses the name that pointed at it. `git log` walks back from where you are standing, so a commit no branch reaches never shows up there. `git reflog` reads a different list: every place `HEAD` has stood, newest first, each line starting with the hash it stood on.",
  "task.1.goal.0": "Replace the top commit with `git commit --amend -m \"add Pip the budgie\"`.",
  "task.1.goal.1": "Read `git log --oneline`. Three commits, and `add bird` is not one of them.",
  "task.1.goal.2": "Read `git reflog`. `HEAD` stood on `add bird`, and that line still starts with its hash.",
  "task.1.goal.3": "Copy that hash and `git checkout` it. You end up standing on the commit `git log` could not see.",
  "task.2.title": "Give it a name",
  "task.2.concept": "git branch <name> <hash>",
  "task.2.context": "The amend has already happened here. The commit that added `bird.txt` is the one you want back, and `git log` cannot see it.\n\nStanding on it does not keep it: step away and it is out of reach again. A commit is lost when no name points at it, so the repair is to point a name at it. `git branch <name> <hash>` puts a branch on any commit you can name, and the reflog is where the hash comes from.",
  "task.2.goal.0": "Read `git log --oneline`. The commit that added `bird.txt` is not on the list.",
  "task.2.goal.1": "Read `git reflog` and find the line `commit: add bird`. The hash it starts with is that commit.",
  "task.2.goal.2": "Put a branch called `rescue` on it with `git branch rescue <hash>`.",
  "task.2.goal.3": "Move onto it with `git checkout rescue`. `add bird` has a name again, so `git log` can reach it.",
  "task.3.title": "Find a lost commit - recap",
  "task.3.concept": "Recap",
  "task.3.context": "Two lists, and only one of them forgets.",
  "task.3.summaryIntro": "`git log` answers one question: what can this branch still reach. `git reflog` answers a different one: where has `HEAD` been. The second list keeps commits the first has already forgotten, so a commit you replaced ten minutes ago is still findable by its hash.",
  "task.3.summaryItems.0.title": "`git reflog` - ",
  "task.3.summaryItems.0.text": "lists every place `HEAD` has stood, newest first, each line starting with the hash it stood on.",
  "task.3.summaryItems.1.title": "`HEAD@{0}`, `HEAD@{1}` - ",
  "task.3.summaryItems.1.text": "the position of each line in that listing. Read them to find the line you want; the hash at the start of that line is what you type.",
  "task.3.summaryItems.2.title": "`git checkout <hash>` - ",
  "task.3.summaryItems.2.text": "stands you on a commit no branch reaches, so you can look at it before deciding.",
  "task.3.summaryItems.3.title": "`git branch <name> <hash>` - ",
  "task.3.summaryItems.3.text": "points a name at that commit, which is what stops it being lost.",
  "task.3.summaryClose": "Next you meet `git reset`, which moves a branch name backwards on purpose. This is the list you read when it takes more than you meant.",
  "concept.gt-reflog.term": "Reflog",
  "concept.gt-reflog.def": "Git's own record of every place `HEAD` has stood, newest first, which keeps a commit findable by its hash after no branch reaches it any more.",
  "meta.label": "Fixing mistakes · Find a lost commit",
  "task.1.files.0.text": "Mia, tabby, 4 years old.",
  "task.1.files.1.text": "Rex, collie, 2 years old.",
  "task.1.files.2.text": "Pip, budgie, 1 year old.",
  "task.2.files.0.text": "Mia, tabby, 4 years old.",
  "task.2.files.1.text": "Rex, collie, 2 years old.",
  "task.2.files.2.text": "Pip, budgie, 1 year old."
}
