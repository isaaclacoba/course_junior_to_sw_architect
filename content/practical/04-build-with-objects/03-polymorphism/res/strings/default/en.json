{
  "hero.eyebrow": "Part four · Build with objects",
  "hero.title": "Why many versions?",
  "intro.0": "Why write several small classes that do the same kind of job, instead of one method with a branch for each case? The one big method feels tidier - until it grows again every time something new shows up. Here you'll see how letting each type carry its own version keeps that from happening.",
  "task.1.title": "A branch for every animal",
  "task.1.concept": "The type switch",
  "task.1.context": "Without polymorphism, one method carries every animal as a branch. Write a `Zoo` with a method `Sound(string kind)` that returns `\"Meow\"` for `\"cat\"`, `\"Woof\"` for `\"dog\"`, and `\"?\"` for anything else. It works - but every new animal means another `if` in here.",
  "task.1.goal.0": "Write a `Zoo` class with `string Sound(string kind)` covering \"cat\", \"dog\", and anything else.",
  "task.1.goal.1": "Main asks for the cat, so the output is Meow.",
  "task.2.title": "Let each animal carry its own sound",
  "task.2.concept": "One call, many behaviours",
  "task.2.context": "Instead of one method with branches, give each animal its own class behind `IAnimal`. Write `Cat` and `Dog` that implement it. Now the call `a.Speak()` is written once, yet makes a different sound depending on which animal `a` holds - no `kind` argument, no branch.",
  "task.2.goal.0": "Write `Cat : IAnimal` (says Meow) and `Dog : IAnimal` (says Woof).",
  "task.2.goal.1": "Main calls the identical `.Speak()` on each, so the output is two lines: Meow then Woof.",
  "task.3.title": "One loop, the whole pen",
  "task.3.concept": "Polymorphism over a list",
  "task.3.context": "Because every animal keeps the same promise, you can put different ones in a single `List<IAnimal>` and walk them with one loop. Write the `Main`: build a list with a `Cat` and a `Dog`, then loop it and print what each one says. The loop never asks which animal it is holding.",
  "task.3.goal.0": "In Main, build a `List<IAnimal>` holding a `Cat` then a `Dog`, and loop it printing each `Speak()`.",
  "task.3.goal.1": "So the output is two lines: Meow then Woof.",
  "task.4.title": "Pick the animal at runtime",
  "task.4.concept": "Runtime selection",
  "task.4.context": "Which animal you get can be decided while the program runs. Write a `Shelter` with a method `Adopt(string kind)` that returns a new `Dog` when `kind` is `\"dog\"`, otherwise a new `Cat`. The caller then makes one plain `Speak` call and never needs to know which it got.",
  "task.4.goal.0": "Write a `Shelter` with `IAnimal Adopt(string kind)` - a Dog for \"dog\", otherwise a Cat.",
  "task.4.goal.1": "Main adopts a dog and it speaks, so the output is Woof.",
  "task.5.title": "Add an animal, leave the loop alone",
  "task.5.concept": "Open to new behaviour",
  "task.5.context": "In the first task, a new animal meant editing the one method. Here the list and loop in `Main` are finished. Write a new `Owl` class that keeps the `IAnimal` promise and says `\"Hoot\"`; it joins the pen and the loop never changes.",
  "task.5.goal.0": "Write an `Owl : IAnimal` whose `Speak()` returns `\"Hoot\"`.",
  "task.5.goal.1": "It is already in the pen, so the three-line output is Meow, Woof, Hoot.",
  "task.6.title": "Why many versions? - recap",
  "task.6.concept": "Recap",
  "task.6.context": "A handful of small classes beats one method full of branches.",
  "task.6.summaryIntro": "Polymorphism lets each type carry its own behaviour, so one call site does the right thing for whatever it is given.",
  "task.6.summaryItems.0.title": "The type switch - ",
  "task.6.summaryItems.0.text": "one method full of `if`s grows and gets risky with every kind.",
  "task.6.summaryItems.1.title": "One call, many behaviours - ",
  "task.6.summaryItems.1.text": "`a.Speak()` is written once but resolves to the real object's version.",
  "task.6.summaryItems.2.title": "Over a list - ",
  "task.6.summaryItems.2.text": "a single loop walks a `List<IAnimal>` of mixed kinds.",
  "task.6.summaryItems.3.title": "Runtime selection - ",
  "task.6.summaryItems.3.text": "choose the implementation while the program runs.",
  "task.6.summaryItems.4.title": "Open to new behaviour - ",
  "task.6.summaryItems.4.text": "add a class; the loop never changes.",
  "task.6.summaryClose": "Next: Inherit or compose?"
}
