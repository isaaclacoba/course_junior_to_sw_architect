{
  "hero.eyebrow": "Part three · Know the language",
  "hero.title": "Data shapes",
  "intro.0": "Collections hold things; now build the things they hold. Five small programs you write from scratch and run, covering the everyday ways C# packages data: properties, a computed property, an enum, a struct, and a record. No blanks to fill - write the type, run it, match the output.",
  "task.1.title": "Give it a property",
  "task.1.concept": "Property",
  "task.1.context": "A property is data on an object you can read and write, with `{ get; set; }` doing the work behind the scenes. Give the `Cat` a `Name` property so the code below can set it and print it.",
  "task.1.goal.0": "Add a `Name` property (with `get` and `set`) to `Cat` so it holds a string.",
  "task.1.goal.1": "Main sets Name to Mittens and prints it, so the output is Mittens.",
  "task.2.title": "A property that computes",
  "task.2.concept": "Computed property",
  "task.2.context": "A property can compute its value from other data and skip the setter. Give `Dog` an `Age`, plus a read-only `HumanYears` that returns `Age * 7`.",
  "task.2.goal.0": "Add an `Age` property (`get`/`set`), and a read-only `HumanYears` property that returns `Age * 7`.",
  "task.2.goal.1": "Main sets Age to 3, so HumanYears prints 21.",
  "task.3.title": "A fixed set of options",
  "task.3.concept": "enum",
  "task.3.context": "An `enum` is a type with a fixed set of named options - no stray strings to mistype. Define a `Mood` enum, then turn each mood into something the pet says.",
  "task.3.goal.0": "Define an `enum Mood` with `Sleepy`, `Hungry` and `Playful`.",
  "task.3.goal.1": "In `Say`, return `\"zzz\"` for Sleepy, `\"feed me\"` for Hungry, `\"woof\"` for Playful.",
  "task.3.goal.2": "Main calls Say(Mood.Hungry), so the output is feed me.",
  "task.4.title": "A value that copies",
  "task.4.concept": "struct",
  "task.4.context": "A `struct` is a value type: assigning it makes a fresh copy, so changing the copy never touches the original. (A `class` would share one object, and the original would change too.) Define `Treats` as a struct and watch the copy stay separate.",
  "task.4.goal.0": "Define `Treats` as a `struct` with a public `int Count` field.",
  "task.4.goal.1": "Main copies the treats, changes the copy to 99, and prints the original's Count - which should still be 3.",
  "task.5.title": "Two that are equal by value",
  "task.5.concept": "record",
  "task.5.context": "A `record` is a data type that compares by value: two records with the same contents are equal. (A `class` compares by reference, so two separate objects are never equal even with identical data.) Define an `Animal` record and check two equal ones.",
  "task.5.goal.0": "Define `record Animal(string Name, int Legs);`.",
  "task.5.goal.1": "Main makes two animals with the same Name and Legs and prints whether they are equal - which should be True.",
  "concept.pr-enum.term": "enum",
  "concept.pr-enum.def": "A type that is one of a fixed set of named options, like a small labelled list of choices.",
  "concept.pr-struct.term": "struct",
  "concept.pr-struct.def": "A small data bundle that is copied whole when you assign or pass it, so two variables never share the same one.",
  "concept.pr-record.term": "record",
  "concept.pr-record.def": "A type meant to hold data, where two of them count as equal when their values match, not just when they are the same object."
}
