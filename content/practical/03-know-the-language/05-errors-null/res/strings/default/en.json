{
  "hero.eyebrow": "Part three · Know the language",
  "hero.title": "Exception handling",
  "intro.0": "When something goes wrong, a program can stop dead - unless you handle it. Write and run each piece: catch failures with try/catch, read what failed from the message, clean up with finally, raise your own with throw, and reach for values safely with the null-safety operators ?? and ?. (you met null in Foundations).",
  "task.1.title": "Catch a problem",
  "task.1.concept": "try / catch",
  "task.1.context": "Some operations fail - dividing by zero, reading past the end of a list. A failure throws an **exception** that stops the normal flow. Put the risky code in a `try` block and handle the failure in a `catch` block, so the program keeps running instead of crashing.",
  "task.1.goal.0": "Wrap the division in a `try`, and `catch (DivideByZeroException)`.",
  "task.1.goal.1": "In the catch, print `cannot split treats among zero dogs`. That should be the output.",
  "task.2.title": "What went wrong",
  "task.2.concept": "The message",
  "task.2.context": "An exception is an object that carries details about the failure, including a `Message`. Catch it into a variable - `catch (Exception e)` - and read `e.Message` to find out what happened.",
  "task.2.goal.0": "In the `catch`, print the exception's `Message`.",
  "task.2.goal.1": "The thrown message is `the bowl is empty`, so that should be the output.",
  "task.3.title": "Always clean up",
  "task.3.concept": "finally",
  "task.3.context": "A `finally` block runs no matter what - whether the `try` finished cleanly or a `catch` handled a failure. It is where you put steps that must always happen, like closing a file or locking up.",
  "task.3.goal.0": "Add a `finally` block that prints `locking the cat flap`.",
  "task.3.goal.1": "The output should be feeding the cat, then locking the cat flap.",
  "task.4.title": "Raise your own",
  "task.4.concept": "throw",
  "task.4.context": "When your own code spots something invalid, it can raise an exception with `throw`. Here a method refuses a negative number of treats; the caller (already written) catches it and prints the message.",
  "task.4.goal.0": "In `Treats`, if `count` is negative, `throw new ArgumentException(\"treats cannot be negative\")`.",
  "task.4.goal.1": "`Main` calls `Treats(-1)` and catches it, so the output should be treats cannot be negative.",
  "task.5.title": "A fallback with ??",
  "task.5.concept": "Null-coalescing",
  "task.5.context": "The `??` operator gives a fallback when the left side is `null`. `name ?? \"stray\"` means: use `name`, but if it is `null`, use `\"stray\"` instead - one short line, no `if` needed. (You met `null` in Foundations.)",
  "task.5.goal.0": "Use `??` so `display` falls back to `\"stray\"` when `name` is `null`.",
  "task.5.goal.1": "`name` is null here, so the output should be stray.",
  "task.6.title": "Safe access with ?.",
  "task.6.concept": "Null-conditional",
  "task.6.context": "Writing `?.` instead of `.` only reaches for the member when the object is not `null`; if it is null, the whole expression is `null` instead of crashing. Here a missing name's length comes back as `null`, not a crash.",
  "task.6.goal.0": "Use `?.` so `length` becomes `null` (not a crash) when `name` is `null`.",
  "task.6.goal.1": "`name` is null here, so the output should be unknown.",
  "task.7.title": "Exception handling recap",
  "task.7.concept": "Recap",
  "task.7.summaryIntro": "Exceptions handle things going wrong; the null-safety operators deal with a value that might not be there. You met `null` itself back in Foundations.",
  "task.7.summaryItems.0.title": "try / catch - ",
  "task.7.summaryItems.0.text": "run risky code in `try`, handle the failure in `catch`.",
  "task.7.summaryItems.1.title": "the message - ",
  "task.7.summaryItems.1.text": "`catch (Exception e)` then `e.Message` tells you what failed.",
  "task.7.summaryItems.2.title": "finally - ",
  "task.7.summaryItems.2.text": "always runs at the end, for cleanup that must not be skipped.",
  "task.7.summaryItems.3.title": "throw - ",
  "task.7.summaryItems.3.text": "raise your own exception when something is invalid.",
  "task.7.summaryItems.4.title": "?? and ?. - ",
  "task.7.summaryItems.4.text": "supply a fallback for null, or access a member safely without crashing.",
  "task.7.summaryClose": "Next in this track: generics - writing your own types that work with any kind of value, like the List<T> you have already used.",
  "concept.pr-exception-handling.term": "try / catch",
  "concept.pr-exception-handling.def": "Keeping a program standing when something fails by catching the error instead of letting it stop everything.",
  "concept.pr-finally.term": "finally",
  "concept.pr-finally.def": "A block that always runs after a try, whether it succeeded or failed, to clean up.",
  "concept.pr-throw.term": "throw",
  "concept.pr-throw.def": "Raising your own error to signal that something is wrong and stop the current path."
}
